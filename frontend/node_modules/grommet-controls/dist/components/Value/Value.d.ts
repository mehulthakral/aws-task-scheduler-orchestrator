/// <reference types="react" />
import { IValueProps } from './ValueProps';
/**
 * A single value visualization with a label<br/>
 * `import { Value } from 'grommet-controls';`<br/>
 * `<Value value={...} label={...} />`<br/>
 */
declare const Value: {
    ({ label, value, size, weight, gap, units, color, }: IValueProps): JSX.Element;
    defaultProps: {
        label: any;
        value: any;
        weight: string;
        gap: string;
        size: string;
        color: any;
    };
};
export { Value };
