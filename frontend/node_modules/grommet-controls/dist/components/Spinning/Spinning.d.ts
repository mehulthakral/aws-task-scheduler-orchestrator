/// <reference types="react" />
import { ISpinningProps } from './SpinningProps';
/** A spinning process indicator<br/>
 * `import { Spinning } from 'grommet-controls';`<br/>
 * `<Spinning />`<br/>
 */
declare const Spinning: {
    ({ kind, fadeIn, color, size }: ISpinningProps): JSX.Element;
    defaultProps: {
        kind: string;
        fadeIn: string;
        color: any;
        size: string;
    };
};
export { Spinning };
