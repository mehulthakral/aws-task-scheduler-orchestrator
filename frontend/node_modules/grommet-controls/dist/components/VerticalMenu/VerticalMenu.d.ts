import { Component } from 'react';
import { IMenuItem, IVerticalMenuProps } from './VerticalMenuProps';
interface IVerticalMenuState {
    expandedItems?: string[];
    originalExpandAll?: boolean;
    search?: string;
    items?: IMenuItem[];
    filteredItems?: IMenuItem[];
    collapsibleItems?: IMenuItem[];
    allExpanded?: boolean;
    expandAll?: boolean;
}
/** Hierarchical collapsible menu
 * ```$ npm install grommet-controls\n
 * import { VerticalMenu } from 'grommet-controls';\n
 * <VerticalMenu items={...} onSelect={...} />
 *```
 */
declare class VerticalMenu extends Component<IVerticalMenuProps, IVerticalMenuState> {
    state: IVerticalMenuState;
    static getDerivedStateFromProps(nextProps: IVerticalMenuProps, prevState?: IVerticalMenuState): IVerticalMenuState;
    onMenuChange: (id: string, expanded: boolean) => void;
    renderItem: (item: any, level?: number) => JSX.Element;
    render(): JSX.Element;
}
export { VerticalMenu };
